import 'dart:html';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        ListTile(
          title: Center(child: Text('Halaman Login & Signin sampai sabtu malam masih banyak error, jadi saya hanya mengunmpulkan seadanya')),
          ),
      ],
    );
  }
}